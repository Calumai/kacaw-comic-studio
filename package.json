{
  "name": "image-coordinate-workbench",
  "version": "0.1.0",
  "private": true,
  "description": "Zero-dependency tests for the local image coordinate workbench.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "check": "node --check core.js && node --check app.js && node --check server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
